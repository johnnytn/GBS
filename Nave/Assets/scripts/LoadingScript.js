#pragma strict

//var velocidade:float;



function Update () {

	//var Movimento = velocidade * Time.deltaTime;
	//transform.Translate(Vector3.left * Movimento);
	
	//if(transform.position.x < -98){
		
		//transform.position = new Vector3(283,transform.position.y,transform.position.z);
				
	//}	
}

